---
name: ci
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-lint:
    name: backend / lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.3"
          check-latest: true

      # golangci-lint is the single source of truth for Go linting: `run`
      # also reports the `formatters` (gofmt/goimports) as issues and runs
      # go vet (govet), so there are no separate vet/fmt steps.
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: --timeout=5m

  backend-test:
    name: backend / build + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.3"
          check-latest: true

      - name: build
        run: go build ./...

      - name: test
        run: go test -race -count=1 ./...

  frontend:
    name: frontend / lint + typecheck + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v6

      - name: setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: install
        run: npm ci

      # Biome is the single lint+format tool for the SPA (replaces ESLint +
      # Prettier); `biome ci` checks both. tsc -b still owns type checking.
      - name: lint + format (biome)
        run: npm run ci

      - name: typecheck
        run: npm run typecheck

      - name: build
        run: npm run build

      - name: upload bundle
        uses: actions/upload-artifact@v7
        with:
          name: frontend-dist
          path: frontend/dist
          if-no-files-found: error
          retention-days: 7

  docker:
    name: docker / build (smoke test)
    runs-on: ubuntu-latest
    needs: [backend-test, frontend]
    # Skip on main pushes — release.yaml builds + pushes there.
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v6

      - name: download frontend bundle
        uses: actions/download-artifact@v8
        with:
          name: frontend-dist
          path: frontend/dist

      - name: setup buildx
        uses: docker/setup-buildx-action@v4

      - name: build image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          tags: cheshmhayash:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

  helm:
    name: helm / lint + template
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: setup helm
        uses: azure/setup-helm@v5
        with:
          version: v4.1.4

      - name: helm lint
        run: helm lint chart/cheshmhayash-chart

      - name: helm template (defaults)
        run: helm template cheshmhayash chart/cheshmhayash-chart > /dev/null
